# 🔄 OOP Polymorphism in PHP

## 🔹 1. Definition
**Polymorphism** means **objects of different classes can respond to the same method call in their own way**.  
It allows code to be flexible, reusable, and easier to maintain.

---

## 🔹 2. Key Concepts

| Concept | Meaning |
|---------|---------|
| Overriding | A child class provides its own version of a parent method |
| Interfaces | Define a set of methods that a class must implement |
| Same method, different behavior | Different classes can have a method with the same name, but each behaves differently |

---

## 🔹 3. Why Use Polymorphism?

✅ Makes code flexible and reusable  
✅ Allows treating different objects uniformly  
✅ Lets you extend functionality without changing existing code  

---

## 🔹 4. Tips

- Use **inheritance** or **interfaces** to achieve polymorphism  
- Override parent methods in child classes to give different behavior  
- Think: *“Different objects can do the same thing, but in their own way”*
